import { defineConfig } from 'vite'

/**
 * IMPORTANT:
 * - Pour tester localement, laisse base = '/'
 * - Pour déployer sur GitHub Pages (https://<user>.github.io/<repo>/),
 *   modifie la valeur en '/<repo>/'
 */
export default defineConfig({
  base: '/',
})
